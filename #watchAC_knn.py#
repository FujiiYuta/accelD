import pandas as pd

# csvファイルのimportと一緒にカラムを設定
accelD = pd.read_csv('watchAC_sample.csv', names=['userAccelerate.x', 'userAccelerate.y', 'userAccelerate.z'])
print(accelD)

X = accelD.data      # 説明変数
Y = accelD.target    # 目的変数



